## Notes on environments

This codebase is tested on Python 3.13.x (author uses 3.13.7). If you see packaging errors on older Python versions,
use Python 3.13+.

## Figures

Figures are stored under `figures/`. The analysis scripts regenerate the figure PDFs without hard-coded figure numbers.
See `figures/scripts to reporduce figures.txt` for the exact commands.
